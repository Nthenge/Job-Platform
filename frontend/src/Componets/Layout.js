import Sidebar from './Sidebar';
import JobsData from './JobsData';

const Layout = () => {
  return (
    <div className="layout">
      <Sidebar />
      <JobsData />
    </div>
  );
};

export default Layout;
